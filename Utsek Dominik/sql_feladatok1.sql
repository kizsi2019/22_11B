1. feladat
 a, SELECT AVG(standard_cost) AS átlag FROM products;
 b, SELECT AVG(standard_cost) AS átlag FROM products WHERE list_price <30;
 c, SELECT AVG(standard_cost) AS átlag FROM products WHERE category LIKE '%Sauces%';
